services:
  app:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/prelegal.db
    volumes:
      - ./data:/app/data
